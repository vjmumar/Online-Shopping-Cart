export type TCategory = {
	label: string;
	type: string;
};

export type TCartProduct = {
	quantity: number;
	productName: string;
	id: string;
	unitPrice: number;
	origPrice: number;
	imageUrl: string;
};
